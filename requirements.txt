# NSEBOT Python dependencies
# Install: python -m pip install -r "C:\Users\manve\Downloads\NSEBOT\requirements.txt"

# Core HTTP + timezone
requests>=2.31.0
pytz>=2024.1

# Environment / secrets
python-dotenv>=1.0.1
pyotp>=2.9.0

# Scheduling
APScheduler>=3.10.4

# Dhan SDK (official)
dhanhq>=2.0.1

# Telegram
python-telegram-bot==21.5

# Dashboard (FastAPI HTML served by dashboard_server.py)

# Optional: Upstox SDK (uncomment if using Upstox as primary)
# upstox-python-sdk>=2.8.0

# Chart data (Chrome-free OHLC fetching)
yfinance>=0.2.40

# Chart data — tvDatafeed for MCX-native real-time OHLC (requires TV_USERNAME + TV_PASSWORD in .env)
# Not on PyPI; install from GitHub:
tvdatafeed @ git+https://github.com/rongardF/tvdatafeed.git

# Web scraping (Moneycontrol fallback fetcher)
beautifulsoup4>=4.12.0
playwright>=1.44.0

# Dev / testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
